<UserControl x:Class="Gomoku_Client.View.Notification"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             Width="400">

    <UserControl.Resources>
        <!-- animation (in) -->
        <Storyboard x:Key="SlideIn">
            <DoubleAnimation Storyboard.TargetName="NotificationBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="450" To="0" Duration="0:0:0.4">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseOut"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="NotificationBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="0" To="1" Duration="0:0:0.3"/>
        </Storyboard>

        <!-- out -->
        <Storyboard x:Key="SlideOut">
            <DoubleAnimation Storyboard.TargetName="NotificationBorder"
                           Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                           From="0" To="450" Duration="0:0:0.2">
                <DoubleAnimation.EasingFunction>
                    <CubicEase EasingMode="EaseIn"/>
                </DoubleAnimation.EasingFunction>
            </DoubleAnimation>
            <DoubleAnimation Storyboard.TargetName="NotificationBorder"
                           Storyboard.TargetProperty="Opacity"
                           From="1" To="0" Duration="0:0:0.25"/>
        </Storyboard>

        <!-- button style -->
        <Style x:Key="AcceptButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#00D946"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Segoe UI Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="20,12">
                            <Border.Effect>
                                <DropShadowEffect Color="#00D946" BlurRadius="15" ShadowDepth="0" Opacity="0.5"/>
                            </Border.Effect>
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#00FF52"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#00B83A"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style x:Key="DeclineButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#4A4A4A"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontFamily" Value="Segoe UI Black"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                Padding="20,12">
                            <ContentPresenter HorizontalAlignment="Center"
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#5A5A5A"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#3A3A3A"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>
    <!-- border -->
    <Border x:Name="NotificationBorder"
            Background="#1A1F2E" 
            BorderThickness="2"
            Margin="0"
            RenderTransformOrigin="0.5,0.5">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0.5" EndPoint="0.75,0.5">
                <GradientStop Color="#FF4655" Offset="0"/>
                <GradientStop Color="#152635" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.RenderTransform>
            <TranslateTransform X="450"/>
        </Border.RenderTransform>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="5" Opacity="0.5"/>
        </Border.Effect>

        <Grid>
            <Grid.RowDefinitions>
                <!-- title bar -->
                <RowDefinition Height="Auto"/>
                <!-- content -->
                <RowDefinition Height="*"/>
                <!-- action -->
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- title bar -->
            <Border Grid.Row="0" 
                    Padding="25,20">
                <Border.Background>
                    <LinearGradientBrush StartPoint="0,0.5" EndPoint="0.75,0.5">
                        <GradientStop Color="#FF4655" Offset="0"/>
                        <GradientStop Color="#152635" Offset="1"/>
                    </LinearGradientBrush>
                </Border.Background>
                <Grid>
                    <TextBlock x:Name="TitleText"
                               Text="THÔNG BÁO" 
                               FontSize="20" 
                               FontWeight="Bold" 
                               FontFamily="Segoe UI Black"
                               Foreground="White"
                               VerticalAlignment="Center"/>

                    <!-- close notif -->
                    <Button x:Name="CloseBtn"
                            Width="30" Height="30"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Background="Transparent"
                            BorderThickness="0"
                            Cursor="Hand"
                            Click="CloseBtn_Click">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Background" Value="Transparent"/>
                                <Setter Property="Template">
                                    <Setter.Value>
                                        <ControlTemplate TargetType="Button">
                                            <Border Background="{TemplateBinding Background}">
                                                <ContentPresenter HorizontalAlignment="Center"
                                                                VerticalAlignment="Center"/>
                                            </Border>
                                        </ControlTemplate>
                                    </Setter.Value>
                                </Setter>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Background" Value="#33000000"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                        <Path Stroke="White"
                              StrokeThickness="2.5"
                              Data="M0,0 L10,10 M10,0 L0,10"/>
                    </Button>
                </Grid>
            </Border>

            <!-- content -->
            <Border Grid.Row="1" 
                    Padding="30,25"
                    Background="#0F1923">
                <ScrollViewer VerticalScrollBarVisibility="Auto" MaxHeight="300">
                    <TextBlock x:Name="MessageText"
                               Text="Đây là nội dung thông báo của bạn. Bạn có thể viết nội dung dài ở đây." 
                               FontSize="15" 
                               Foreground="#ECECEC"
                               TextWrapping="Wrap"
                               LineHeight="24"/>
                </ScrollViewer>
            </Border>

            <!-- action -->
            <Border x:Name="ActionButtonsContainer"
                    Grid.Row="2" 
                    Padding="30,20,30,25"
                    Background="#0F1923">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- acp -->
                    <Button Grid.Column="0"
                            x:Name="AcceptButton"
                            Content="✓ ĐỒNG Ý"
                            Style="{StaticResource AcceptButtonStyle}"
                            Click="AcceptButton_Click"/>

                    <!-- decl -->
                    <Button Grid.Column="2"
                            x:Name="DeclineButton"
                            Content="✕ HỦY BỎ"
                            Style="{StaticResource DeclineButtonStyle}"
                            Click="DeclineButton_Click"/>
                </Grid>
            </Border>
        </Grid>
    </Border>
</UserControl>